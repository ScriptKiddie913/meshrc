{
  "name": "sdmesh-client",
  "version": "1.0.0",
  "description": "Secure De-centralized Mesh - client (identity, local encrypted state, CLI). Talks to a headless sdmesh-server on Render.",
  "main": "src/index.js",
  "type": "commonjs",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "setup": "node --no-warnings src/setup.js",
    "start": "node --no-warnings src/index.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "license": "MIT"
}
